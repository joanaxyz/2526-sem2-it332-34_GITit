# Goal: Shop Contract and Cache Ownership

Use Krypton Execution to execute `docs/goals/shop-contract-cache-ownership/PLAN.md`.

Core rules:
- Treat `PLAN.md` as the source plan.
- Preserve intent, ownership, contract, cutover, evidence, and kill criteria.
- Do not add a new dominant path without deleting the displaced path.
- Capture exact backend request/response evidence and a player-visible purchase/cache trace.
- Preserve every unrelated dirty-worktree byte.
- Say `implemented but unproven` if target-perspective evidence cannot be captured.
