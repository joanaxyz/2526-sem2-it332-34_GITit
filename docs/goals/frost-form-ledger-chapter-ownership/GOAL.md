# Goal: Frost Form Ledger Chapter Ownership

Use Krypton Execution to execute `docs/goals/frost-form-ledger-chapter-ownership/PLAN.md`.

Core rules:
- Treat `PLAN.md` as the source plan.
- Preserve intent, ownership, contract, cutover, evidence, and kill criteria.
- Do not add a new dominant path without deleting the displaced monolith.
- Preserve the current Frost, aggregate Adventure, derived form-Challenge, and generated-target content hashes from `PRE_SLICE_BASELINE.json`.
- Preserve every non-slice dirty path and explicitly protected caller/support file from `PROTECTED_BASELINE.json`.
- Use the persistent `verify_evidence.py` as the terminal content and worktree-preservation authority.
- Capture acceptance evidence from the curriculum maintainer's perspective.
- Say "implemented but unproven" if that evidence cannot be captured.
