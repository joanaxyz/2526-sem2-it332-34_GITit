# Goal: Content Editor Workflow Ownership — Slice 3

Use Krypton Execution to execute `docs/goals/content-editor-workflow-ownership/PLAN.md`.

Core rules:
- Treat `PLAN.md` as the source plan.
- Preserve workflow ownership, external contracts, cutover, accessibility, and evidence criteria.
- Delete the page-owned workflow path atomically; do not leave duplicate draft/mutation logic or a compatibility barrel.
- Keep backend validation/publish truth, backend API/schema/serializer/migrations, and existing frontend API/model utilities unchanged. Backend runtime may only centralize create/update destination precedence and return null official IDs before staff enforcement as specified in Task 3a; tests may add that POST/PATCH regression plus architecture coverage.
- Preserve every out-of-scope hash and every pre-existing shared guard hunk recorded in `PRE_SLICE_BASELINE.md`.
- Correct the pre-existing edit-lesson command-form lookup bug through the effective loaded/new kind and prove zero calls for a loaded lesson.
- Keep component prop contracts narrow: no controller object/spread, and only Diagnostics may receive the full form.
- Capture protected-route request/persistence and durable desktop/mobile browser evidence against dedicated checked ports and an identity-verified disposable database session. Fail closed before mutation on any readiness, API-base, or identity mismatch; clean up only recorded PIDs and the verified session child.
- Say "implemented but unproven" if that full-stack evidence cannot be captured.
