# Goal: First-Game Tutorial Overlays

Use Krypton Execution to execute `docs/goals/first-game-tutorial-overlays/PLAN.md`.

Core rules:
- Treat PLAN.md as the source plan.
- Preserve intent, ownership, contract, cutover, evidence, and kill criteria.
- Replace the existing all-at-once Challenge renderer with the shared sequential path; do not keep duplicate tour engines.
- Do not modify or format any pre-existing dirty path outside the explicitly allowed files.
- Capture real Adventure and Challenge browser evidence from a player's perspective.
- Use plain Challenge terminology (`Scenario`, `Objective`, `Required values`) and prove its copy-to-terminal/paste path.
- Say “implemented but unproven” if visible first-entry, interaction, and persistence evidence cannot be captured.
