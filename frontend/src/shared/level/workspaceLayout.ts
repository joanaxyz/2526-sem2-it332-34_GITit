/**
 * Shared gameplay-workspace sizing.
 *
 * Adventure keeps a dedicated battle-stage row. The terminal is allowed to
 * remain compact; repository feedback and context keep the rest of the
 * workspace.
 */
export const WORKSPACE_BATTLE_STAGE_ROW = 'clamp(16rem, 38vh, 24rem)'
