/** localStorage keys for persisted workspace layout/preferences.
 *
 * Shared across the challenge and adventure surfaces so a learner's choices
 * (e.g. the project-files collapse state) follow them between the two. */

export const PROJECT_FILES_OPEN_KEY = 'workspace:project-files-open'
