# Shared level runtime

Reusable runtime helpers for challenge/adventure command sessions live here.
Feature folders keep product-specific API/cache behavior, while this package owns
small cross-mode rules such as sparse repository-state patch merging.
