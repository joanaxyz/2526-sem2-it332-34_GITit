# GIT it! Frontend

React + TypeScript + Vite student-facing client for the GIT it! current-release MVP.

The app is feature-sliced under `src/features`, uses Tailwind CSS design tokens, TanStack Query for server state, React Router for protected routes, and React Flow for data-driven DAG rendering.
